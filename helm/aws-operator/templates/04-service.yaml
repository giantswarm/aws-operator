apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.resource.default.name | tpl }}
  namespace: {{ .Values.resource.default.namespace | tpl }}
  labels:
    app: {{ .Values.resource.default.name | tpl }}
  annotations:
    prometheus.io/scrape: "true"
spec:
  ports:
  - port: 8000
  selector:
    app: {{ .Values.resource.default.name | tpl }}
