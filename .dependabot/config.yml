version: 1
update_configs:
  - package_manager: "go:modules"
    directory: "/"
    update_schedule: "weekly"
    default_reviewers:
      - "giantswarm/team-firecracker"
    ignored_updates:
      - match:
          dependency_name: "k8s.io/*"
          version_requirement: ">=0.17.0"
